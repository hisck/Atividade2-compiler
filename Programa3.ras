program minimo;
    var a,b,x : integer;
    
    function minimo(a, b: integer): integer;
    begin
        if ( a < b) then
            begin
                minimo := a;
            end
        else
            begin
                minimo := b;
            end;
    end;

begin
    read(a);
    read(b);
    x := minimo(a, b);
    write(x);
end.